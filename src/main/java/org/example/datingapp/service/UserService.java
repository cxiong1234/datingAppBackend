package org.example.datingapp.service;

import org.example.datingapp.model.User;
import org.springframework.stereotype.Service;

@Service
public class UserService {

    public User register(User newUser) {

        return newUser;
    }
}